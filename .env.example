# En local, deja esto apuntando a tu json-server local:
VITE_API_URL=http://localhost:3001

# En Render (variable de entorno del servicio del FRONTEND), cambia
# el valor por la URL real del servicio donde desplegaste server.cjs:
# VITE_API_URL=https://aa-cuestionarios-api.onrender.com
